using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//  Skeleton Simulation RIG contains all structural informatino for the virtual body. (See join hierarchy figure 1)
public class SkeletonSimulation : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
