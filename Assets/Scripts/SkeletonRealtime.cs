using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//  The Realtime Skeleton works as a bind between Control and Simulation RIGs
public class SkeletonRealtime : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
